
import Button from '@mui/material/Button';

const EditButton = ({children}) => {
  return (
      <Button 
        variant="contained"

      >
        {children}
      </Button>
  );
}

export default EditButton